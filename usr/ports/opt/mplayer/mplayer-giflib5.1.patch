--- a/libvo/vo_gif89a.c	2014-01-18 21:39:07.000000000 +0000
+++ b/libvo/vo_gif89a.c	2014-06-02 13:59:06.638781067 +0000
@@ -75,6 +75,9 @@
 #define MakeMapObject GifMakeMapObject
 #define FreeMapObject GifFreeMapObject
 #define QuantizeBuffer GifQuantizeBuffer
+#if GIFLIB_MINOR >= 1
+#define EGifCloseFile(a) EGifCloseFile(a, NULL)
+#endif
 #endif
 
 // how many frames per second we are aiming for during output.
--- a/libmpdemux/demux_gif.c	2014-06-02 14:19:15.861811749 +0000
+++ b/libmpdemux/demux_gif.c	2014-06-02 14:19:55.908369057 +0000
@@ -50,6 +50,10 @@
 #define DGifOpenFileHandle(a) DGifOpenFileHandle(a, NULL)
 #define GifError() (gif ? gif->Error : 0)
 #define GifErrorString() GifErrorString(gif->Error)
+#if GIFLIB_MINOR >= 1
+#define DGifCloseFile(a) DGifCloseFile(a, NULL)
+#endif
+
 #endif
 
 /* >= 4.2 prior GIFLIB did not have MAJOR/MINOR defines */
