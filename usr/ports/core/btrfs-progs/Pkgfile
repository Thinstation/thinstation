# Description: Filesystem utilities for btrfs
# URL:         http://btrfs.wiki.kernel.org
# Maintainer:  CRUX System Team, core-ports at crux dot nu
# Depends on:  util-linux e2fsprogs lzo

name=btrfs-progs
version=3.12
release=1
source=(https://www.kernel.org/pub/linux/kernel/people/mason/btrfs-progs/btrfs-progs-v$version.tar.xz)

build() {
    cd $name-v$version
    make CFLAGS="$CFLAGS"
    make prefix=/usr bindir=/sbin DESTDIR=$PKG install
}
