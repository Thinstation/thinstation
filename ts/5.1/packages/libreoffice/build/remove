#!/bin/sh

installdir=./packages/libreoffice

rm -r $installdir/{etc,opt,bin}
rm -r $installdir/lib/{application-registry,applications,firefox,icons,licenses,mime,mime-info}
rm $installdir/build/{installed,license}
