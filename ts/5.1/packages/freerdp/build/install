#!/bin/sh

INSTALLDIR=./packages/freerdp
EXTRADIR=$INSTALLDIR/build/extra
TMPDIR=./build-freerdp

for i in freerdp ; do
	repackage $i $TMPDIR
	cp -Prf $TMPDIR/$i/* $INSTALLDIR/.
	rm -rf $TMPDIR
done
cp -Prf $EXTRADIR/* $INSTALLDIR/.

returnval=$?

exit $returnval
