#!/bin/sh

INSTALLDIR=./packages/icons-tango
TMPDIR=./build-icons-tango

for i in tango-icon-theme; do
	repackage $i -t $TMPDIR
	cp -Prf $TMPDIR/$i/* $INSTALLDIR/.
	rm -rf $TMPDIR
done

rm -rf $INSTALLDIR/lib/locale

returnval=$?

exit $returnval
