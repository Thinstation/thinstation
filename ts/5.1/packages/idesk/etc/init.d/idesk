#! /bin/sh

. $TS_GLOBAL
. /etc/idesk.functions
case "$1" in  
init)
    if ! pkg_initialized $PACKAGE; then
	pkg_set_init_flag $PACKAGE
	mkdir $HOME/.idesktop
	cp /etc/idesk.tpl $HOME/.ideskrc
	idesk_icons
	idesk_params
    fi
    ;;
  *)
    exit 1
    ;;
esac
exit 0
