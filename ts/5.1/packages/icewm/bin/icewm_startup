. /etc/thinstation.env
. $TS_GLOBAL
. /etc/icewm.functions

$WMNAME & >> $LOGFILE 2>&1

wmautostart="$HOME/.xinitrc"
if [ -e $wmautostart ] ; then
    . $wmautostart
fi
if [ -e $WKDIR/$WMNAME.autostart ] ; then
    . $WKDIR/$WMNAME.autostart
fi
wait
