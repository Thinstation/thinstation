#!/bin/sh

INSTALLDIR=./packages/icons-hicolor
TMPDIR=./build-icons-hicolor

for i in hicolor-icon-theme ; do
	repackage $i -t $TMPDIR
	cp -Prf $TMPDIR/$i/* $INSTALLDIR/.
	rm -rf $TMPDIR
done

returnval=$?

exit $returnval
