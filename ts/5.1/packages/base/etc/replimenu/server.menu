caption = Input Box
colorscheme = 2
runonexit = echo "RM_SERVER='"$RM_SERVER"'" > /tmp/RM_SERVER

begin item
	name = SERVER
	bullet =
	icaption = Enter Server Address (ip or host):
	type = input | var | notempty | auto
end

exitafterauto

br
