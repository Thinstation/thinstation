caption = Input Box
colorscheme = 2
runonexit = echo "RM_USER='"$RM_USER"'" > /tmp/RM_USER

begin item
	name = USER
	bullet =
	icaption = Enter Username:
	type = input | var | notempty | auto
end

exitafterauto

br
