#!/bin/sh

. $TS_GLOBAL

case "$1" in
	init)
	if ! pkg_initialized $PACKAGE; then
		pkg_set_init_flag $PACKAGE
			if [ ! -e $HOME/.local/share/medit-1 ] ; then
			mkdir -p $HOME/.local/share/medit-1
			cp /etc/medit-1/* $HOME/.local/share/medit-1/.
			fi
	fi
	;;
	*)
	exit 1
	;;
esac

exit 0

