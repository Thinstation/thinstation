#! /bin/sh

. /etc/thinstation.global

case "$1" in
init)
 if ! pkg_initialized $PACKAGE; then
	pkg_set_init_flag $PACKAGE
	if [ ! -e $HOME/.wine ] ; then
		mkdir -p $HOME/.wine
		cp /etc/wine/* $HOME/.wine/.
	fi
 fi
;;
esac

exit 0
