#! /bin/sh

. $TS_GLOBAL

case "$1" in  
init)
    if ! pkg_initialized $PACKAGE; then
	pkg_set_init_flag $PACKAGE
	if [ ! -e $HOME/.gtkrc-2.0 ] ; then
	    cp /etc/gtk-2.0/gtkrc-2.0.tpl $HOME/.gtkrc-2.0
	fi
    fi
    ;;
help)
    echo "Usage: $0 init"
    ;;
  *)
    exit 1
    ;;
esac

exit 0
