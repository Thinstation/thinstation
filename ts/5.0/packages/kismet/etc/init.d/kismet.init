#! /bin/sh

. /etc/thinstation.global

case "$1" in
init)
 if ! pkg_initialized $PACKAGE; then
	pkg_set_init_flag $PACKAGE
	if [ ! -e $HOME/.kismet/kismet_ui.conf ] ; then
		mkdir -p $HOME/.kismet
		cp /etc/kismet_ui.conf $HOME/.kismet/kismet_ui.conf
	fi
 fi
;;
esac

exit 0
