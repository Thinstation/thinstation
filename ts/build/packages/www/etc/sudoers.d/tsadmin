## user is allowed to execute halt and reboot 
lighttpd    ALL=(tsadmin) NOPASSWD: /bin/sudo halt, /bin/sudo reboot, /bin/sudo poweroff
tsadmin    ALL=(root) NOPASSWD: /sbin/halt, /sbin/reboot, /sbin/poweroff

