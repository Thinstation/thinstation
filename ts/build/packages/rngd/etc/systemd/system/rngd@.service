[Unit]
Description=Hardware RNG Entropy Gatherer Daemon
Conflicts=shutdown.target
After=systemd-random-seed.service

[Service]
Type=forking
ExecStart=/sbin/rngd -W 5% -f
ExecStopPost=/bin/systemctl start rngd-backup
SuccessExitStatus=66
