#!/bin/sh

installdir=./packages/vhusbip
export PACKAGE=vhusbip
mkdir $installdir/bin
cp ./wget_tmp/vhusbdi386  $installdir/bin/vhusbdi386
chmod +x $installdir/bin/vhusbdi386
cp -R $installdir/build/extra/etc $installdir/etc
repackage -e
returnval=$?
exit $returnval
