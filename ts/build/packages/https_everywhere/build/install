#!/bin/sh

gzname=`basename $1`
INSTALLDIR=./packages/https_everywhere/etc/TS.default/extensions/
ID=https-everywhere-eff\@eff.org

mkdir -p $INSTALLDIR

mv wget_tmp/extension/ $INSTALLDIR/$ID.xpi
let returnval=returnval+$?

exit $returnval
