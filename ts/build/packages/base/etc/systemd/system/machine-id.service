[Unit]
Description=Parse /proc/cmdline for machine-id and set it
DefaultDependencies=no
Before=systemd-machine-id-setup.service
Before=systemd-journald.socket getty.target

[Service]
Type=oneshot
ExecStart=/etc/init.d/machine-id
RemainAfterExit=yes
Slice=init.scope

[Install]
WantedBy=basic.target
