pkcs11-tool -r -d 03 --type cert --output-file /tmp/cert.der
openssl x509 -in /tmp/cert.der -inform DER -out /tmp/cert.pem

sbsign \
  --engine pkcs11 \
  --key 'pkcs11:manufacturer=piv_II;id=%03' \
  --cert /tmp/cert.pem  \
  --output $1.signed \
  $1
