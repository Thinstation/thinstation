IMPORTANT

The thinstation.conf files should be secured and only accessible by administrative users.

Thinstation.conf files are effectively executed as scripts.  This means if unauthorized users have access to them,
they could write malicious scripts.
