# Description: Library of PNG support functions.
# URL:         http://www.libpng.org
# Maintainer:  Tilman Sauerbeck, tilman at crux dot nu
# Depends on:  zlib

name=libpng12
version=1.2.46
release=1
source=(http://download.sourceforge.net/libpng/libpng-$version.tar.xz)

build() {
    cd libpng-$version

    ./configure --prefix=/usr/local --mandir=/usr/local/man


    make
    make DESTDIR=$PKG install
    rm -rf $PKG/usr/local/{man,bin,include}
    rm -rf $PKG/usr/local/lib/{pkgconfig,libpng.so}
    find $PKG -name \*.la -delete
    find $PKG -name \*.a -delete
}
