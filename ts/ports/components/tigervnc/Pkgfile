# Description: A free remote control software package derived from VNC
# URL: http://tigervnc.sourceforge.net/
# Maintainer: 
# Depends on: xorg, xorg-imake, xorg-gccmakedep (?)

name=tigervnc
version=1.3.0
release=1
source=(http://sourceforge.net/projects/tigervnc/files/tigervnc/$version/$name-$version.tar.bz2)

build() {
        cd $name-$version

	CFLAGS="$CFLAGS -Wno-error=deprecated-declarations" cmake -G "Unix Makefiles" -DCMAKE_INSTALL_PREFIX=/usr -DENABLE_NLS=OFF

        CFLAGS="$CFLAGS -Wno-error=deprecated-declarations" make
        make DESTDIR=$PKG install
}
