#
# This indirect map uses fuse.sshfs
# The keys must be exchanged in advance
# This mounts things as root it is dangerous, therefore disabled
#

* -fstype=fuse,rw,nodev,nonempty,noatime,allow_other,reconnect,max_read=65536 :sshfs\#root@&\:/
