# Description: Set of not-so good-quality plugins for gstreamer under the LGPL.
# URL: http://gstreamer.freedesktop.org/modules/gst-plugins-good.html
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Matt Housh, jaeger at morpheus dot net
# Depends on: gst-plugins-base lame libmpeg2 x264 libcdio opencore-amr

name=gst-plugins-ugly
version=0.10.19
release=1
source=(http://gstreamer.freedesktop.org/src/$name/$name-$version.tar.xz)

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--mandir=/usr/man \
		--disable-debug \
		--disable-nls \
		--disable-dependency-tracking \
		--disable-valgrind \
		--disable-examples \
		--enable-shared \
		--disable-static

	make
	make DESTDIR=$PKG install
}
